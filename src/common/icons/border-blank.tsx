import type { HTMLAttributes } from "react";

export default function BorderBlankIcon(
  props: HTMLAttributes<HTMLSpanElement>,
) {
  return (
    <i
      className="border_blank_line"
      {...props}
    />
  );
}
