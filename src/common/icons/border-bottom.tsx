import type { HTMLAttributes } from "react";

export default function BorderBottomIcon(
  props: HTMLAttributes<HTMLSpanElement>,
) {
  return (
    <i
      className="border_bottom_line"
      {...props}
    />
  );
}
